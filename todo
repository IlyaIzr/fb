добавить предыдущее вэлью
если при маунте с вэлью и лишними ключами пришли лишние ключи-значения, вернуть их в дате сабмита
либо
поля с неподходящими полями рендерятся как пустые, плюс глоб настройка чтобы это отключить
after mount updates lifecycle handler
box shadows

submitInvisible для полей
? multiple rendered fields {} chages

try form reset, modal, select, onInput


Changes from first build:
fields now is object of fields, where object key is field key
buttons config is now object
field.tabIndex -> field.tab
field.rowIndex -> field.row
no more config.tabs.buttons settings

select = with clearable === false should not be allowed to clear options